#include "kgmBase64.h"

const char base64chars[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";

kgmBase64::kgmBase64()
{
}

bool kgmBase::encode(kgmByteArray& encoded, sint8* buffer, sint32 length)
{
  if(!buffer || !length)
    return false;

  return true;
}

bool kgmBase::decode(kgmByteArray& decoded, sint8* buffer, sint32 length)
{
  if(!buffer || !length)
    return false;

  return true;
}
